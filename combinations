from itertools import combinations

def print_combinations(numbers):
    for r in range(1, len(numbers) + 1):
        for combination in combinations(numbers, r):
            print(combination)

# 
numbers_list = [1, 2, 3]
print_combinations(numbers_list)
